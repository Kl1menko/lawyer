{
  "name": "ladyzhyn-lawyer-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate-sitemap.mjs",
    "preview": "vite preview",
    "sitemap": "node scripts/generate-sitemap.mjs"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
